# MongoDB connection string
MONGO_URI=your-mongodb-uri

# OpenAI API key (https://platform.openai.com/api-keys)
OPENAI_API_KEY=your-openai-key

# Secret used to sign JWT tokens — use a long random string
JWT_SECRET=any-random-secret

# Stripe secret key (https://dashboard.stripe.com/apikeys)
STRIPE_KEY=your-stripe-key

# Stripe Price ID for the subscription product
STRIPE_PRICE_ID=price_xxxxxxxxxxxx

# Public URL of the deployed site (no trailing slash), used for Stripe redirect URLs
SITE_URL=https://your-site.com

# Port the server listens on (defaults to 5000 if not set)
PORT=5000
